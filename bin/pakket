#!/usr/bin/env perl

# ABSTRACT: The Pakket meta packaging application
# PODNAME: pakket

use v5.22;
use warnings;
use strict;

use Pakket;

my $app = Pakket->new();

# Data::Dump::dd $app;
# Data::Dump::dd [sort $app->command_names()];
# Data::Dump::dd [sort $app->command_plugins];
# my ($cmd, $opt, $args) = Pakket::Command::Install->prepare($app);

exit ($app->run() // 0);
